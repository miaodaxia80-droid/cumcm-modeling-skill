# 贡献指南

这个项目需要数学建模、数值计算、数据工程、论文写作和 Agent 工程的共同开发。优先贡献能被复现的失败案例和最小修复，不以增加文档体积为目标。

## 适合贡献的内容

- 已结束历届真题的合法来源、元数据和清洗规则；
- 能重现审题、公式、单位、约束、求解或论文错误的最小案例；
- 公式转录、单位分析、数据清洗、结果模板和提交包的确定性验证器；
- 在隐藏年份/题目上的盲测结果、评分和失败归因；
- 现有规则或知识模块中的错误、重复、冲突和过度约束修复。

## 数据与版权

1. 只提交你有权再分发的数据、题面、附件和论文。
2. 不确定再分发权时，只提交来源 URL、元数据、checksum 和本地构建脚本。
3. 禁止提交当届尚未结束的竞赛题解、他人未授权论文、个人信息、凭据、Cookie 或会话内容。
4. 大文件请先讨论 Git LFS 或可重建下载方案，不要直接向 Git 历史堆入大量二进制文件。

## 真题案例的最小信息

一个可用的 Issue 或 PR 应当包含：

- 竞赛、年份、题号和公开来源；
- 可观察失败，例如“公式分母丢失”或“时间序列分割泄漏”；
- 最小复现步骤与相关文件；
- 预期不变式、参考解或人工审核依据；
- 错误影响哪个结果或评分项；
- 数据使用与再分发边界。

## Pull Request 原则

- 修正根因，不只修一个真题的表面症状。
- 如果现有规则、标准库或脚本已能处理，优先复用。
- 新增规则必须说明它能跨题复用，并给出至少一个会在回归时失败的可运行检查。
- 不将单题答案、年份数值或某篇论文的结论硬编码进通用 Skill。
- 修改 `SKILL.md` 时保持入口简洁；只在对应阶段需要时才加载详细引用。
- 文档声称必须与真实盲测状态一致。

## 提交前检查

```bash
python3 ~/.codex/skills/.system/skill-creator/scripts/quick_validate.py .
python3 scripts/init_task.py --help
python3 scripts/verify_task.py --help
python3 scripts/package_submission.py --help
```

同时检查：

- 无凭据、个人路径和未授权材料；
- 修改的脚本已真实运行；
- 盲测结论包含题号、评分和可复现证据；
- 没有用开发集或公开题解泄漏隐藏测试集。
