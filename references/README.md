# references/ 用途

存放外部参考资料，本 skill 不读取其内容，仅作为人工核对/扩展用：

- 当年官方竞赛文件（竞赛章程、论文格式规范、提交要求 PDF）
- 往年真题及附件数据
- 优秀获奖论文（学习用）
- 自定义规范（用户自己整理的格式要求）

## 使用约定

- **用户提供了当年官方格式规范时，它优先于本 skill 的 `rules/paper-format.md`**（当次指令压过默认）
- Agent 在解题时**不需要**通读本目录；只有用户点名「按我给的格式文件来」时才打开对应文件
- 本目录不进知识库索引，不影响触发与流程
